{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(api|login|logout|add|remove|update|export|signup)(.*)",
      "destination": "/api/index.js"
    }
  ]
}
